export const offScreenSpaceBefore = 20; // prevent "hard edges" effect
export const gameWidth = 100; // tiles
export const gameHeight = 100; // tiles

export const useDevicePixelRatio = true; // if true, changes dimensions based on devicePixelRatio
export const baseTileSize = 6; // before applying devicePixelRatio
export const baseBorderWidth = 1; // before applying devicePixelRatio

export const deadColor = '#222';
export const aliveColor = '#3b3';
export const borderColor = '#333';
